"use strict"; // treat all JS code as newer version
//alert(3+3) if you run this in console of chrome browser u will get 6 but in nodejs you have different syntax for it
//null is a standalone value in javascript
//if u check datatype of null it will be object while undefined is itself a datatype
let age =19
console.log(typeof "panther")
console.log(typeof age)